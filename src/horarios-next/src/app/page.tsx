import Horarios from '@/components/Horarios/Horarios';

/*
 * Conforme solicitado, esta página contém apenas a tela de
 * Cadastro de Horários. A navbar/menu lateral do sistema (Fatec)
 * será desenvolvida separadamente e deve envolver este conteúdo
 * (por exemplo, dentro de um layout compartilhado do Next.js).
 */
export default function Home() {
  return <Horarios />;
}
