import { RecursosListagem } from "@/components/recursos/recursos-listagem";

export default function RecursosPage() {
    return (
        <div className="flex min-h-screen w-full flex-col bg-white">
            <RecursosListagem />
        </div>
    );
}
