import { CursosListagem } from "@/components/cursos/curso-listagem";

export default function CursosPage() {
    return (
        <div className="flex min-h-screen w-full flex-col bg-white">
            <CursosListagem />
        </div>
    );
}